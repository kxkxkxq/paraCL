;
while(?) {}

print ?;
print -?;
print +?;

print ? - 1;
print 1 - ?;

print ? - ?;
print ? + ?;

print ? / ?;
print ? * ?;
print ? % ?;

print ? == 1;
print 1 == ?;

print !?;

print ? < ?;
print ? > ?;

print ? == ?;
print ? != ?;

print ? <= ?;
print ? >= ?;

print ? && ?;
print ? || ?;
