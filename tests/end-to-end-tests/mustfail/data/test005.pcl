n = ?;

if (n < 0)
    m = -n;
if(n >= 0)
    m = n;

{
    i = 1;

    while (i <= m)
    {
        if (m % i == 0)
        {
            if(n <= 0)
                 -i;
            if(n > 0)
                i;
        }
        i = i + 1;
    }
}